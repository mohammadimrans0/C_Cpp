#include<stdio.h>

int main(){
    
    printf("hello hi\nI am a programmer\n");

    int john = 100, kane = 200;

    printf("%d %d\n", john, kane); // for printing int we need to mention "%d"

    float william = 2.557;

    printf("%0.3f\n", william); // for printing float we need to mention "%0.1f"

    char name = 'I';

    printf("%c", name); // for printing character we need to mention "%c" and char only took one character

    return 0;
}

// special character

// \n => new line
// \t => tab line (tab mean 4 space)

// \ and % can't print solely